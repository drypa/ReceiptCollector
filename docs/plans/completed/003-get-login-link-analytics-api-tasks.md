# Задачи: Перевод GetLoginLinkCommand на вызов Analytics API

## Задача 1: Добавить ANALYTICS_URL в конфигурацию бота

**Описание:** Добавить переменную окружения `ANALYTICS_URL` в конфигурацию бота для возможности вызова Analytics API.

**Файлы для изменения:**
- `bot/config/config.go` — добавить поле `AnalyticsUrl` в структуру `Config` и загрузку из env
- `.env` — добавить `ANALYTICS_URL=http://analytics:8080`
- `.env.example` — добавить `ANALYTICS_URL=`

**Критерии приёмки:**
- Конфигурация загружается без ошибок
- Значение `AnalyticsUrl` доступно в коде
- При отсутствии переменной — понятная ошибка или значение по умолчанию

**Подсказки:**
- Пример: смотри существующие поля в `bot/config/config.go` (например, `BackendGrpcAddress`)
- Формат: `os.Getenv("ANALYTICS_URL")` или использовать библиотеку для env (если она уже используется в проекте)

---

## Задача 2: Создать HTTP клиент для Analytics API

**Описание:** Создать HTTP клиент в боте для вызова Analytics API endpoint `GET /api/users/auth-link-request?telegramId={id}`.

**Файлы для изменения:**
- Создать `bot/analytics/client.go` — новый файл с HTTP клиентом

**Критерии приёмки:**
- Клиент делает корректный GET запрос к `/api/users/auth-link-request?telegramId={id}`
- Возвращает URL (строку) или ошибку
- Обрабатывает случаи: сервер недоступен, неверный ответ, таймаут

**Подсказки:**
- Пример HTTP клиента: ищи в `backend/clients/` или в `Analytics/` проекте
- Использовать стандартный `net/http`
- Конфигурация: `baseURL` из `config.AnalyticsUrl`

---

## Задача 3: Обновить GetLoginLinkCommand

**Описание:** Изменить команду `/login` в боте для использования нового HTTP клиента вместо gRPC вызова.

**Файлы для изменения:**
- `bot/commands/login.go:14-21` — заменить `grpcClient.GetLoginLink(ctx, telegramId)` на вызов analytics client

**Критерии приёмки:**
- Бот возвращает ссылку, полученную от Analytics API
- Сообщение об ошибке при недоступности Analytics
- Код компилируется без ошибок

**Подсказки:**
- Текущий код: `bot/commands/login.go:14-21`
- Нужно передать `analyticsClient` в команду (через конструктор или зависимость)

---

## Задача 4: Удалить неиспользуемый gRPC код

**Описание:** Удалить неиспользуемый gRPC метод `GetLoginLink` из бота и из backend.

**Файлы для изменения:**
- `bot/backend/grpc.go` — удалить метод `GetLoginLink` если он больше не вызывается
- `backend/users/processor.go:35` — удалить реализацию gRPC метода `GetLoginLink`
- `backend/proto/*.proto` — удалить определение RPC `GetLoginLink` (если оно там есть)
- `backend/users/*.go` — удалить связанные методы/функции если они стали неиспользуемыми

**Критерии приёмки:**
- Код компилируется без ошибок
- gRPC метод `GetLoginLink` больше не вызывается в боте
- gRPC метод `GetLoginLink` удалён из backend

**Подсказки:**
- Проверить все вызовы: `grep -r "GetLoginLink" bot/`
- Проверить все вызовы в backend: `grep -r "GetLoginLink" backend/`
- Если метод используется в других командах — не удалять
- После удаления пересобрать proto файлы: `go generate ./...` или `make proto`

---

## Задача 5: Проверить работоспособность

**Описание:** Убедиться, что интеграция работает корректно.

**Файлы для изменения:**
- Нет

**Критерии приёмки:**
- Запустить бота локально
- Выполнить команду `/login`
- Проверить в логах, что запрос уходит на Analytics API
- Ссылка генерируется и отправляется пользователю

**Подсказки:**
- Для локального тестирования: `./up.dev.sh` поднимает все сервисы
- analytics API доступен на порту, указанном в `ANALYTICS_URL`